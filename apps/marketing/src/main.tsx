import React from "react";
import ReactDOM from "react-dom/client";
import { BrowserRouter } from "react-router-dom";
import App from "./App";
import "./index.css";
import { initEnhancedAnalytics, setupClickTracking } from "./lib/analytics";

// Initialize enhanced analytics tracking with engagement scoring
initEnhancedAnalytics();
setupClickTracking();

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </React.StrictMode>,
);
